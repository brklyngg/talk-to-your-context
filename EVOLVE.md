# Workflow Evolution Log

- [brilliant moves] Run /council_deep before building any sizeable architectural plan — external models reading the full repo + spec will reframe the problem if the team is anchored on a specific solution. This session: GPT-5.5-pro and Gemini-3.1-pro both said "the running-log middle layer solves the wrong layer; the prompt is the root cause." Saved a multi-day implementation. When OpenRouter credits are out, run an inline `python3 - <<EOF` against direct OPENAI_API_KEY + GEMINI_API_KEY (gpt-5.5-pro + gemini-3.1-pro-preview) — bypasses the council.py wrapper cleanly.
- [process patterns] When two plans are in flight in adjacent chats, do a sibling-plan crosscheck before final review — a sibling change shipping VAD upstream collapsed the cost rationale of this plan and forced a refocus from "cost suspension" to "graceful continuity." Catching that overlap mid-planning saved a wasted implementation pass.
- [process patterns] Gary's iteration cycle for non-trivial plans: plan → /review-plan → crosscheck-with-sibling → /review-plan again → Ultraplan handoff (cloud refinement, returns as `<ultraplan>` paste) → implement. Each stage may reveal the previous stage's flaws; don't treat any single review as the finish line. When he says "send to Ultraplan," it's not finalization — it's another refinement round.
- [process patterns] For UX-quality audits, read the per-call transcript JSON (`~/.hermes/voice-transcripts/*.json`), not the diagnostic NDJSON. Transcripts reveal fragmentation, mis-transcription, and language drift; NDJSON reveals event-flow bugs. Quantify failures across ≥10 recent calls with file IDs and counts before proposing fixes — that's what makes specs land at /review-plan time.
- [process patterns] When Gary's instinct ("more telemetry first") competes with a possible root-cause fix, he wants an honest "slam dunk?" judgment — not the methodologically-pure path by default. /review-plan with external doc research is the right arbiter: if community guides corroborate a root cause, ship the fix with a slim diagnostic strip in the same deploy rather than gating on a separate diagnose-then-fix round trip.
- [process patterns] Gary will invoke /review-plan repeatedly until the plan reaches the depth he wants — don't treat the first ExitPlanMode as the finish line. Each iteration he expects: external doc research grounded in the current month, integration of the project's stated vision, and a thoughtful pass for missed killer features. The biggest wins came from his vision prompt ("magic of talk-to-your-context", outdoor 5G + indoor wifi + deep-context survival) which surfaced the spawn+long-poll/resume design that wasn't in the original plan.
- [quality bar] When deploying changes that touch a live service Gary uses daily, expect post-deploy iteration — he tests immediately, screenshots failures (even with iPhone status bars showing time), and surfaces ambiguity quickly. Be ready to diagnose from `logs/calls/<conv_id>.ndjson`-style structured event logs first; transcripts are user-facing, not for debugging.
- [communication style] Gary answers AskUserQuestion options with prose questions, not just selections — he wants dialog, not multiple choice. When his answer reveals a confusion, explain architecture (with a diagram if useful) before re-asking.
- [process patterns] Verify the target repo against the working directory before trusting a handoff/spec doc — handoff doc pointed at ~/.hermes/hermes-agent but the actual project was talk-to-your-context. Gary will redirect with a one-liner like "the handoff doc fucked up"; flag the mismatch on first sign of confusion rather than digging further into the wrong target.
- [quality bar] Single feature commit on main, but only push after explicit confirmation in-message — the harness blocks `git push origin main` even with prior plan-mode approval. Treat each push as a fresh authorization.
